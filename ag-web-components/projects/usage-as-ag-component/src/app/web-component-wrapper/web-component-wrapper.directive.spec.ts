import { WebComponentWrapperDirective } from './web-component-wrapper.directive';

describe('WebComponentWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new WebComponentWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
